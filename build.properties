<!-- ======================================================================= -->
<!--  Application settings                                                   -->
<!-- ======================================================================= -->

app.name=WeldEstimator
app.namespace=com.tripotamus.WeldEstimator
app.version=0.0.1

<!-- ======================================================================= -->
<!--  Directory mappings                                                     -->
<!-- ======================================================================= -->

src.dir=${basedir}/src
lib.dir=${basedir}/lib
build.dir=${basedir}/build
build.classes.dir=${build.dir}/classes
build.lib.dir=${build.dir}/lib
metadata.dir=${basedir}/metadata

<!-- ======================================================================= -->
<!--  File mappings                                                          -->
<!-- ======================================================================= -->

buildnumber.file=${metadata.dir}/build.number

<!-- ======================================================================= -->
<!--  Library dependency settings                                            -->
<!-- ======================================================================= -->

clojure=clojure.jar
clojure-contrib=clojure-contrib.jar
db=hsqldb.jar
miglayout=miglayout-3.7.3.1-swing.jar

clojure.jar=${lib.dir}/${clojure}
clojure-contrib.jar=${lib.dir}/${clojure-contrib}
db.jar=${lib.dir}/${db}
miglayout.jar=${lib.dir}/${miglayout}
